# 方案评估与实施路线

## 结论

原方案产品边界正确，但一期同时覆盖数据源、选股、复盘、报告、三种导出、React 和 Electron，难以为每条业务规则建立可验收证据。开发改为“独立仓库 + 纵向切片”：每一阶段都必须有界面、API、持久化和测试闭环。

## 已核对的风险

1. `meirixuangu.skill` 的实际打分不只是文档所述的“五维”，还包含量价、均线、换手、振幅、板块动量和运行时段微调。第一版评分引擎按实际脚本语义分维度移植，不用文档摘要猜测。
2. 免费网页行情接口可能限流或改字段，不能成为核心测试的前置条件。数据采集将通过适配器接入，评分引擎只接收标准化事实。
3. 原备份中存在明文 Key 风险；原文件保留在项目仓库外，不复制、不提交。
4. “梭哈区”等是个人标签，数据模型可保留，但界面始终与自动交易能力隔离。
5. 手机与电脑各自本地存储，共享的只能是 JSON Schema、规则文档和固定测试向量。固定股票池只是参考数据，不作为个股查询准入条件。

## 分期

### M0（当前）：可运行研究闭环

- 原创 React 工作台；
- 本地 FastAPI + SQLite；
- 策略 JSON Schema 与“群友原版 v1”；
- 评分试算、评分解释、预选股保存和状态更新；
- 离线自动测试。

### M1：真实数据与复盘

- [x] 腾讯实时行情适配器；
- [x] 来源、抓取时间、交易时间和降级状态；
- [x] 可选参考池批量扫描与 SQLite 行情快照；
- [x] 全市场股票搜索与用户自定义自选列表；
- [x] 腾讯前复权日线适配器与 MA5/10/20；
- [x] 扫描运行记录、轮动池（25 只上限、单板块最多 3 只）和批量加入预选股；
- [x] 预选股建立 1/5/20 个交易日核验任务，扫描后自动核验，亦可通过 API 手动触发；
- [x] 评分结果保存策略规则指纹，避免同版本号下规则变更无法追溯；
- [x] AKShare 可选备用行情/日线适配器（腾讯失败或核心字段缺失时回退）；
- [ ] 09:20 每日预选和盘后自动任务（当前由本地启动后的扫描入口触发）；

### M2：深度报告与桌面封装

- [x] 个股深研第一版：腾讯行情、前复权 K 线、MA/MACD/RSI、支撑压力；
- [x] 八维火箭评分、数据缺失标识、价格区间和操作参考；
- [x] React 个股深研页面与 SQLite 分析快照（`analysis_reports` / `analysis_facts`）；
- [x] 知行指数十因子、六维雷达、近120日走势、日周线联合诊断和证据/矛盾提示；
- [x] 东方财富资金流、财务增速、行业热度和新闻适配器，结果带来源/时间/降级状态并保存分析事实；
- [x] 核心/增强/旧版评分状态拆分、十因子覆盖率与可信度、算法版本和规则指纹；
- [x] 增强源一次重试、SQLite 最近成功缓存与 stale 回退；
- [x] 历史分析报告列表与本地报告回看入口；
- [x] 市场复盘页面：最新扫描广度、涨幅/评分靠前、行业横截面和轮动池；
- [x] 候选表现核验页面：1/5/20 日状态、收益和手动核验入口；
- [x] 数据源状态页：安装、可访问、数据有效、响应耗时、最近成功时间和手动测试；
- [x] 历史扫描切换与 1/5/20 日胜率、平均收益汇总；
- [x] 个股分析数据新鲜度：行情、日线、资金流、财务、行业和新闻的更新时间、缓存状态与过期提示；
- [ ] K 线图形化增强、股东数据适配器和报告导出；
- 亮色个股报告，HTML/PDF 导出；
- Electron 打包，后端仍只监听回环地址；
- 本地备份与恢复。

### 已取消的范围

- 条件选股：取消独立入口，股票查询统一使用全市场搜索与用户自建自选池；
- 达人消息：取消 M3 规划，不在当前个人本地研究工具中维护消息推送链路；
- 手机端：暂不排期，先完成桌面端数据可信度、图形分析、研究日志、核验统计和备份导出。

## 验收原则

- AI 和网络关闭后，本地历史与评分仍可用；
- 任何候选都能追溯策略版本、输入、各维度得分和时间；
- 评分同时记录策略文件与评分代码的规则指纹；
- 数据缺失明示，不伪造占位；
- 没有券商接口或任何自动下单路径。
